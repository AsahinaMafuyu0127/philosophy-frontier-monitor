# Changelog

本文件记录会影响监测语义的实现变化。

## 0.1.0 - 2026-09-07

- 为源代码发布包增加显式文件白名单，防止本地安装测试、缓存、私人配置或运行产物因忽略规则歧义进入发布附件；
- 新增共用的有界只读 HTTP 重试器：PhilPapers RSS、PhilArchive OAI、OpenAlex 和 Crossref 对 408、
  429、选定 5xx 与传输错误最多尝试三次，默认指数退避 1 秒、2 秒，单请求累计等待上限 10 秒；
  短 `Retry-After` 被遵守，长等待或 OpenAlex 次日重置会立即形成带安全等待秒数的结构化失败。
- Crossref／OpenAlex 在有界重试仍失败后打开仅限本次运行的来源熔断，避免每个候选重复触发同一
  429／5xx；另一来源和已有缓存继续工作，仍需失败来源的候选保持未解决，周报与即时报告标注
  覆盖失败。错误信息不包含请求 URL、查询正文、响应正文或凭据。
- `pull-now` 的私人书目缓存下沉到单个 DOI 与单个规范化题名：重复运行只把未命中的 DOI／题名
  送往 OpenAlex。DOI 正结果保存 24 小时、成功未收录保存 15 分钟；题名批量只缓存正候选 1 小时，
  空结果、传输失败和语义复核不缓存。新增批量缓存命中／实际 API 值数量统计和重复运行回归测试。
- 新增显式 `--report-delivery file`：完整即时报告原子写入私人报告目录，stdout 只返回路径、字符数
  与统计，避免长报告全文进入模型上下文；默认 `inline` 保持不变，两种模式都不写周报状态。
- 即时与周报的默认 PhilPapers feed 加载改为在一次运行内复用同一 HTTP 连接池；逐篇退回核验
  分别复用单一 Crossref、OpenAlex 连接池，减少重复 TCP／TLS 建连但不减少或隐瞒 API 查询，亦
  不改变证据标准。新增连接复用回归测试与报告内数据源能力边界说明。
- `pull-now` 默认增加与周报状态分离的私人 `bibliography-cache.sqlite3`：查询键只保存散列，响应
  只保存最小书目字段。缓存命中仍重新执行窗口、身份与分类匹配，也不抑制以后报告；可用
  `--no-bibliography-cache` 关闭。各查询路径的具体 TTL 见本节新增的细粒度缓存说明。
- 明确周报与 `pull-now` 共用新近性、旧作／重录、作品同一性、早期稿本类型和分类交集标准；即时
  模式的批量查询、候选优先级与退回预算只属于冷启动性能机制，并增加周报
  `confirmed_source_arrival` working-paper 回归测试。
- 新增只读 `pfm scope-estimate` 与兴趣范围资源政策：在启用上位分类全部后代前计算展开总数、净
  新增 feed 和基线影响；新增 11 个以上须提醒并明确确认，超过 50 个还须提供较窄分支方案。命令
  不联网、不修改状态、不默认输出完整后代，也不把 API credits、网络工作和模型 token 混为一谈。
- 配置 schema 升至 `0.5`，新增首次对话引导状态与按范围记录的可选 PhilPapers／PhilPeople 公开
  资料授权；拒绝或暂缓不保留 URL 和范围，旧配置继续兼容且不被视作已经授权。
- 新增只读 `pfm onboarding-status`，在不输出研究方向原文或资料 URL 的情况下区分：新用户需同时
  询问研究方向和账户决定、旧画像只需补问账户决定、已完成引导无需重复询问。

- 流水线版本升至 `0.2.0`：新增 `confirmed_source_arrival`，允许尚未进入 Crossref／OpenAlex 的
  PhilPapers 新近手稿、working paper、preprint、作者接受稿和 forthcoming 文章在完成旧作检查后
  及时推送；`publication_date` 与 `availability_date` 保持分离。
- 作品身份核验从题名／首位作者字面相等升级为证据分层：共同标识符、作者相容、Unicode 重音与
  姓名顺序、题目实词集合、词序和高置信度拼写差异；完全跨语言且无共同标识时保留语义复核。
- `pull-now` 超出逐篇查询预算时优先处理具有近期日期或明确早期稿本状态的候选，其余显式计入
  `fallback_deferred`，不再使整个即时报告失败或把未检查部分冒充零结果。
- 使用真实八分类和默认 50 条逐篇核验预算完成重构后的七日即时运行：2039 条唯一来源记录形成
  345 条候选，87 条由 DOI／题名批量路径解析，36 条由 PhilPapers 新近到达路径解析，最终匹配
  32 篇并明确延期 208 条；运行成功且未推进周报状态。随后补充跨语言／大幅改题候选的语义复核
  闸门并通过自动测试；同日最终真实复跑受 OpenAlex HTTP 429 阻断，仍未推进状态。32 篇结果仍待
  人工抽查旧作误报率。
- 维护者确认项目代码采用 MIT License，并选择 GitHub Private Vulnerability Reporting 作为公开后
  的私密安全报告渠道；远程仓库正式发布前仍须实际启用该功能。
- 流水线版本升至 `0.1.4`：`pull-now` 在 stderr 输出只含阶段与数量的进度，覆盖分类 feed、候选、
  DOI／题名批量核验和每十条逐篇退回；最终机器可读 JSON 继续单独写入 stdout。
- OpenAlex 批量题名过滤改用官方 `title.search.exact`，同时保留规范化题名与首位作者姓氏的本地
  严格复核。真实八分类聚合诊断把批量严格匹配从 72 条提高到 91 条，但没有消除冷启动成本。
- 已完成一次带免费 OpenAlex key 的真实七日即时运行：345 条候选中 266 条逐篇退回，形成 122 个
  唯一书目作品、219 条 unresolved、确认窗口内新作与匹配均为 0；运行成功且周报状态未改变。
- 公开 v0.1 的稳定范围收窄为基线、每周增量监测与漏周补跑；`pull-now` 保留为显式选择的实验命令，
  在取得更可靠的近期候选入口或书目批量接口前不作为稳定默认能力。

- 流水线版本升至 `0.1.3`，增加独立只读 `pfm pull-now`：默认以请求时刻为右端回看七个当地日，
  不要求周报基线或状态数据库；完整读取当前分类 feed 后，以 feed 日期作候选门槛，再以
  Crossref／OpenAlex 日期作新论文门槛。
- 即时报告只在单份报告内部合并同一作品，不查询或写入 `weekly_new_papers` 通知，也不写来源、
  重试、运行历史和检查点；重复即时拉取以及以后周报可以再次出现同一论文。总候选默认上限为
  1000，逐篇退回上限为 50，超限时在相应请求前整体失败而不静默截断。
- 真实 PhilPapers RSS 不提供条目时间字段时，只在内存中从 description 的书目开头提取四位年份
  作为宽候选提示；description 和年份均不保存或输出，且每篇输出仍需独立日期证据。
- 增加 OpenAlex DOI／规范化标题 OR 批量查询，批量结果仍须通过严格题目和首位作者核验；支持从
  进程环境读取可选 `OPENALEX_API_KEY`，并确保 HTTP 错误不回显 key 或查询 URL。

- 流水线版本升至 `0.1.2`，SQLite schema 升至 `4`：新增不可变的活动兴趣画像快照，按漏跑窗口的
  计划交付时刻选择当时生效的版本；同版异内容会失败，避免无痕改写历史匹配语义。
- 活动快照只保存确认／展开／排除分类、feed、taxonomy、版本生效时刻和调度语义，不复制研究方向
  原文、未确认候选或候选理由。文件型旧 schema 在迁移前会建立并验证 SQLite 备份。
- 增加只读 `pfm taxonomy-audit`：检测分类新增、删除、同 ID 改名、父集合、主要父分类、活动状态
  和来源页变化，并计算确认／候选／排除／展开分类及 feed 覆盖受到的影响；审计不会自动迁移或
  修改私人配置。
- 增加 taxonomy 快照保留闸门：相同内容重复导入保持幂等，同一 snapshot ID 的异内容冲突保留旧
  文件并失败；跨 taxonomy 的历史补跑可按画像中的 snapshot ID 加载并重新验证同目录旧快照。
- 配置 schema 升至 `0.4`，把 `catch_up_after_login` 改名为更准确的
  `catch_up_missed_windows`；继续兼容旧字段，但不再把登录瞬间触发视为验收要求。
- 增加只读 `pfm release-check`：检查私人路径忽略规则、Git 跟踪集合、高置信度凭据、公开示例与
  私人画像逐字重复、完整 taxonomy 再分发、许可证、第三方声明和私密安全报告渠道；检查不输出
  秘密值，不初始化 Git，也不执行任何远程发布。
- 将公开 watchlist 改为明确的虚构示例，不再逐字复制本机私人研究方向。
- 增加 `THIRD_PARTY_NOTICES.md`，依据当前锁文件、已安装包元数据和官方上游说明记录直接依赖、
  PhilPapers／PhilArchive、Crossref 与 OpenAlex 的许可和再分发边界。

## 0.1.0 - 2026-09-05

- 将项目目标固定为“自然语言研究方向 → PhilPapers 受控分类集合 → 新论文分类交集 → 周报”；
- 删除论文质量评价、排序和阅读优先级设计；
- 建立 Skill 入口、Python 3.12 项目与锁文件；
- 实现 taxonomy 校验、兴趣映射、层级展开、时间闸门、集合匹配、去重、状态库和周报器；
- 实现 PhilPapers 分类 RSS、PhilArchive OAI-PMH、Crossref 和 OpenAlex 适配器的第一版；
- 实现配置 schema 0.2、首次历史基线、多分类合并、仅处理新增记录和限次待核验重试；
- 实现 `pfm baseline` 与 `pfm weekly-run`，周报先原子写盘，再以单一 SQLite 事务提交状态和检查点；
- 增加重复周窗口保护、远程 Markdown 文本转义、PhilPapers `/rec/` 链接白名单和 Windows
  `tzdata` 依赖；
- 将本地 SQLite schema 升至版本 3，为运行记录增加 taxonomy、兴趣配置、流水线与匹配规则版本，
  并为早期 schema 提供加列迁移；
- 增加认证式 PhilPapers taxonomy 抓取、环境变量凭据、脱敏网络错误、响应边界检查、完整图
  校验和原子化私人快照写入；
- 增加 Git 忽略私人凭据文件读取、中文全角冒号兼容、Cloudflare challenge 独立诊断，以及
  浏览器下载后使用同一验证链的 `pfm taxonomy-import` 降级路径；
- 导入并验收真实 PhilPapers taxonomy：6154 条来源记录规范化为 6153 个可用分类；显式记录一个
  空名称孤立叶记录与三个重名分类组，分类匹配以 ID 而不是名称为唯一键；
- 放宽安全的 PhilPapers 记录链接白名单以接受真实 `/rec/-1566`，同时继续拒绝外部主机、嵌套路径、
  `.` 与 `..` 路径；
- 使用真实 taxonomy 和四个柏拉图分类完成正式历史基线、dry-run 与第一次正式周运行；状态库包含
  1516 条来源记录、1598 条分类关联和一次成功周运行，且重复周窗口保护已实测；
- 此时仍缺每周计划任务；后续同日已经补充缺口检测并在本机启用 Codex 计划任务。
- 增加兴趣标签有限推理顶层设计：区分已确认活动集合与待确认候选集合，按直接蕴含、原典、
  概念邻接、历史接受、当代桥接和方法语境记录推理关系；
- 增加 `minimal`、`adaptive`、`exploratory` 三种推理深度，并禁止根据表达方式给用户贴新手或
  专家标签；
- 配置 schema 升至 `0.3`，增加结构化候选标签和本地调度偏好；解析器仍兼容 `0.2`，且
  `proposed_categories` 永不进入 weekly-run 的活动集合；
- 固定默认调度目标为用户当地时间周一 08:00、登录后补跑和 Codex 失败通知；同时记录 Codex
  本地计划任务要求预定时刻电脑及应用在线。当时登录补跑与多周分窗补齐尚未实现；后续同日已
  增加 `pfm catch-up` 并启用本机 Codex 计划任务。
- 增加兴趣配置扩展时的增量基线：只抓取尚无基线的新分类 feed，在同一事务中登记其当前历史，
  不重置或覆盖旧分类检查点。
- 用户确认候选 1—4 后，将私人兴趣配置升至第 2 版，新增 `Plato: Knowledge and Belief`、
  `Plato: Definition`、`Plato: Theory of Recollection` 和 `Plato: Forms` 四个活动分类；其 911 条
  feed 记录合并为 794 条唯一来源记录并完成增量历史基线。其余三个候选保持待确认，不参与匹配。
- 流水线版本升至 `0.1.1`，增加 `pfm catch-up`：按用户当地计划时刻推导到期窗口，按时间顺序
  补齐连续缺口，并延后属于以后窗口或暂不能分窗的记录；默认八窗口安全上限，多窗口中途失败时
  如实保留并报告已经提交的周次。
- 兴趣配置增加带时区的 `effective_from`；补跑跨越画像版本时，如果旧窗口在新版画像生效前已
  到期且没有旧画像快照，程序会在网络访问前停止，避免追溯改写研究范围。
- 在本机 Codex 中启用每周一 08:00 的“哲学前沿论文周报”任务，实际执行入口为 `pfm catch-up`；
  官方未提供登录事件触发保证，因此仍把“登录瞬间补跑”列为未完全实现的部署语义。
